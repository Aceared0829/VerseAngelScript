如果要覆盖默认前缀 /usr/local
（即头文件将安装在 /usr/local/include，
库文件将安装在 /usr/local/lib），请依次运行
make、make install 和 make uninstall（分别用于构建、
安装与卸载——如下文所述，三条命令都需要
指定该前缀）：

make PREFIX=/tmp
make PREFIX=/tmp install
make PREFIX=/tmp uninstall

当然，若没有目标目录的写入权限，后两条命令
必须以 root 身份执行（对于 /tmp 则无需如此，除非
文件已由他人创建且你没有相应权限）。

无论如何，上述命令会将头文件安装到
'/tmp/include'，并将库文件安装到
'/tmp/lib'。

若想了解为什么在覆盖安装位置时，构建阶段
也必须指定 PREFIX=，请参阅 FAQ 文件。
简而言之：这样可以让你在链接共享库时
更加方便。

给覆盖 prefix 的用户的一点提醒：
--
你当然可以忽略这条建议，但我认为有必要提出：
如果确实要覆盖 prefix，强烈建议不要将其设为
'/usr'。原因是什么？通常，用户自行编译的程序、
库等会安装在 '/usr/local'（有时为 '/opt'）下。
这样做有多项好处：你可以保持 '/usr' 整洁；如果使用
包管理器，也可以明确地说：
“/usr/include 和 /usr/lib 下的文件只属于某个软件包。”
换言之，这样更便于维护完整性并验证一切。最重要的是
（除安全性影响外）：发生名称冲突的可能性更小。如果将来
某个安装到 /usr 的软件包包含了这些文件（例如 AngelScript
的软件包），就会产生问题。
--

最后：我没有办法测试移动设备构建，因此无法验证构建流程
（它和大多数事情一样，确实取决于环境与架构）；我是个不喜欢
“智能”潮流的老派人（况且用“智能”这个词来形容它实在很不恰当）。

联系方式：
如有任何问题或疑虑，请让 Andreas 联系我（目前我没有
愿意公开的邮箱；如果这对其他人也有帮助，我很可能会在自己的
服务器上专门创建一个邮箱）。现阶段，Andreas 可以将
消息转发给我。
